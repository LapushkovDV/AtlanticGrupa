.var
  StrNN,
  Nom,
  NomFull,
  kolAdd,            // кол-во недопечатанных строк на текущей странице
  MainKol,           // фиксированное кол-во строк на первой странице
  KolPage,           // счетчик обработанных строк на текущей странице
  FixKol,            // фиксированное кол-во строк на страницах, начиная со второй
  KolSpSopr,
  FixKolPage,        // допустимое кол-во строк на текущей странице
  breakPage,
  wFillCol10         // заполнение 10-й графы формы (моссой нетто или кол-вом)
             : word;

  KolItog,           // итоговые суммы по страницам
  MassaBrItog,
  MassaNetItog,
  Sum1Itog,
  Sum2Itog,
  Sum3Itog,
  Sum1ValItog,
  Sum2ValItog,
  Sum3ValItog,

  KolItogFull,       // итоговые суммы по документу
  MassaBrItogFull,
  MassaNetItogFull,
  Sum1ItogFull,
  Sum2ItogFull,
  Sum3ItogFull,
  Sum1ValItogFull,
  Sum2ValItogFull,
  Sum3ValItogFull,

  СтНДСНац,
  СтНацБН,
  СтНацСН
//СтНДСВал,
//СтВалБН,
//СтВалСН
             : double;

  NextPage,
  bAutoGrM          // автоматический расчет кол-ва грузовых мест
             : boolean;

  СпСимвол,
  strEmpty,
  Cena_Pr0,
  KolGrM,
  KolOneGrM
             : string;

  wForm
             : word;

  bRunSign
             : boolean;

  sFIO1, sPost1, sFIO2, sPost2, sFIO3, sPost3,
  sFIO4, sPost4, sFIO5, sPost5, sFIO6, sPost6
             : string;
.endvar
.begin

  // инициализация переменных
  oPrnSoprFun.Init(0, 0, wRoundMass2, false, false, false, Massa_Tn, Massa_Kg, Massa_Gr, Cena_Pr, Cena_PrR, NDS_St, RoundGrm);

  wForm    := 8;
  bRunSign := False;

#ifdef _TTN_SKL_ORG_
  #undef _TTN_SKL_ORG_
#end
end.

#include GetPersSign.frn